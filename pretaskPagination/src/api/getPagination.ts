//api 통신

export async function getPagination(props: url) {
  // const {url} = props;
    // const response = await axios.get( url,
      // {
      //   headers: {
      //     'Content-Type': 'application/json',
      //   },
      // },
}
